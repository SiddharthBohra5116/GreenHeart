/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        playfair: ['var(--font-playfair)'],
        dm: ['var(--font-dm)'],
      },
    },
  },
  plugins: [],
}